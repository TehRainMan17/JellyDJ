"""
routers/auth.py — PATCH for auto-provisioning defaults on user login

Find the login() function in routers/auth.py.
After the line:
    _upsert_managed_user(db, jellyfin_user_id, jellyfin_username, is_admin)

Add these lines (provision is a no-op when no defaults are configured):

    # Auto-provision default playlists for this user on every login.
    # provision_user_defaults() is idempotent — it skips any playlist the user
    # already has for that template, so calling it on every login is safe.
    try:
        from routers.admin_defaults import provision_user_defaults
        provision_user_defaults(jellyfin_user_id, db)
    except Exception as _prov_err:
        # Never let provisioning break login — just log it.
        log.warning("Auto-provision on login failed for %s: %s", jellyfin_user_id, _prov_err)

The login function should then look like:

    _upsert_managed_user(db, jellyfin_user_id, jellyfin_username, is_admin)

    # ── NEW: auto-provision default playlists ──────────────────────────────
    try:
        from routers.admin_defaults import provision_user_defaults
        provision_user_defaults(jellyfin_user_id, db)
    except Exception as _prov_err:
        log.warning("Auto-provision on login failed for %s: %s", jellyfin_user_id, _prov_err)
    # ──────────────────────────────────────────────────────────────────────

    access_token, refresh_token = _issue_tokens(
        db, jellyfin_user_id, jellyfin_username, is_admin, jellyfin_token
    )
"""
