"""
backend/main.py — PATCH for registering the admin_defaults router

CHANGE 1: Add import with the other router imports (near line ~28430):

    from routers.admin_defaults import router as admin_defaults_router

CHANGE 2: Register the router with app.include_router().
Find the block of app.include_router() calls and add:

    app.include_router(admin_defaults_router)

Full context (find this section and add the new line):

    from routers import (
        connections, external_apis, indexer, recommender,
        webhooks, discovery, playlists, insights, automation, exclusions,
    )
    from routers.auth import router as auth_router
    from routers.playlist_templates import router as playlist_templates_router
    from routers.user_playlists import router as user_playlists_router
    from routers.admin_defaults import router as admin_defaults_router    ← ADD

    ...

    app.include_router(connections.router)
    app.include_router(external_apis.router)
    # ... all other existing routers ...
    app.include_router(admin_defaults_router)    ← ADD
"""
