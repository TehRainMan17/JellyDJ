
fastapi>=0.111.0,<0.112.0
uvicorn[standard]>=0.30.0
sqlalchemy>=2.0.0
apscheduler>=3.10.0
cryptography>=42.0.0
httpx>=0.27.0
python-dotenv>=1.0.0
pydantic-settings>=2.3.0
pydantic>=2.7.0
# Module 3 — external API adapters
spotipy>=2.23.0
pylast>=5.3.0
musicbrainzngs>=0.7.1
# billboard.py PyPI package name has a dot which can confuse pip on some platforms
# Install from GitHub to be safe:
git+https://github.com/guoguo12/billboard-charts.git
# Module 5 — recommendation engine
numpy>=1.26.0
# Testing
pytest>=8.0.0
# Auth Phase 1 — JWT issuance and verification
python-jose[cryptography]>=3.3.0
# Playlist Import — metadata fetch from Spotify/Tidal/YouTube Music without API keys
yt-dlp>=2024.1.0
# ID3 tag writing for YouTube rips with user-corrected metadata
mutagen>=1.47.0
# Image processing for YouTube rip cover art (crop thumbnail to square)
Pillow>=10.0.0
